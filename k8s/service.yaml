apiVersion: v1
kind: Service

metadata:
  name: mydotnetapp-service

spec:
  type: NodePort

  selector:
    app: mydotnetapp

  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
      nodePort: 30080